src = $(wildcard *.c) # add.c sub.c test.c
obj = $(patsubst %.c, %.o, $(src)) # add.o sub.o test.o

ALL:a.out

a.out:$(obj)
	gcc $(obj) -o a.out

test.o:test.c
	gcc -c test.c -o test.o

add.o:add.c
	gcc -c add.c -o add.o

sub.o:sub.c
	gcc -c sub.c -o sub.o

clean:
	-rm -rf $(obj) a.out
